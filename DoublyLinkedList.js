

let list = null;

function insert(list, value, pos) {
    let node = { 
        prev : null,
        data : value,
        next : null
    };
    if(pos <= 0 && pos > length(list)) return;

    
}